export function OfficeFloor() {
  return (
    <div
      className="lo-floor"
      style={{
        position: "absolute",
        inset: 0,
        borderRadius: 34,
        border: "1px solid var(--lo-line)",
        boxShadow: "var(--lo-shadow)",
      }}
    />
  );
}
